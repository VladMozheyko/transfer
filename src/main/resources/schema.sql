create table account (
    id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name varchar(50) NOT NULL,
    amount DOUBLE NOT NULL
);

--INSERT INTO account VALUES (1, "Vlad", 11000);
--INSERT INTO account VALUES (2, "Katya", 1000 );